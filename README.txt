- In the terminal, head to the directory where the java files PacketSender and PacketReceiver are located

- Compile the java files using `javac *.java`

- Start the server file first, using `java PacketReceiver`

- Once the the server has started, open a new terminal at the same directory and start the client using `java PacketSender`

- Follow the prompts